use crate::shinkai_message_proto::ShinkaiMessage;

// impl Clone for ShinkaiMessage {
//     fn clone(&self) -> Self {
//         let mut new_message = ShinkaiMessage::new();

//         // Manually clone each field.
//         // Replace `self.field.clone()` with appropriate code for each of your fields.
//         new_message.set_field(self.field.clone());
//         // Repeat the above line for each field in your message.

//         new_message
//     }
// }
