{
  "extends": "./node_modules/gts/",
  "ignorePatterns": ["**/*.test.js"],
  "rules": {
    "no-restricted-imports": ["error", {
      "paths": [
        { "name": "fs", "message": "File system is readonly by default." },
        { "name": "fs/promises", "message": "File system is readonly by default."},
        { "name": "node:fs", "message": "File system is readonly by default." },
        { "name": "node:fs/promises", "message": "File system is readonly by default." }
      ]
    }]
  }
}
